{
  "name": "@docsplit/scripts",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "init": "./init-project.sh",
    "import:customers": "node import-masters.js --customers",
    "import:documents": "node import-masters.js --documents",
    "import:offices": "node import-masters.js --offices",
    "import:all": "node import-masters.js --all",
    "backfill:displayname": "ts-node backfill-display-filename.ts"
  },
  "dependencies": {
    "@google-cloud/logging": "^11.2.1",
    "firebase-admin": "^12.7.0",
    "//pdf-lib-pinning-note": "Issue #432 PR-C3b: pdf-page-visual-v2 fingerprint の cross-process determinism は pdf-lib version + 内部 pako 仕様に依存する。caret range だと CI 別日 run で transitive dep が変わる余地があるため exact pin (Codex review #019e1e54 Critical 1 一次対応、plan-side lockfile hash gate は PR-C3c)。",
    "pdf-lib": "1.17.1"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
